## Change

- Purpose:
- Change contract: `openstack-production-datacenter/docs/operations/change-contracts/<feature>.yaml`
- Downstream/upstream boundary:
- Runtime image revision:
- Tests and rollback:

Use `single-repository: <reason>` only when no external contract changes.
